We ran the model five times independently and saved the predictions from each run in a separate result file:

- `result1.txt`
- `result2.txt`
- `result3.txt`
- `result4.txt`
- `result5.txt`

Each result file contains two columns:

- **Column 1:** Ground-truth value
- **Column 2:** Predicted value

Each row corresponds to one sample in the test set.
